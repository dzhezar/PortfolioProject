{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="col-6">
        {{ form_start(form) }}
            {{ form_row(form.name) }}
            <button type="submit" class="btn btn-primary">Submit</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

